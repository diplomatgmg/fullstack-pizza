import styled from "styled-components"
import { fontWeights } from "../../../../styles/theme.ts"

export const CategoryItemStyle = styled.li`
  list-style: none;
  white-space: nowrap;
  font-weight: ${fontWeights.regular};
`
