import { colors } from "../../styles/theme.ts"

const buttonStyle = {
  bgColor: colors.white,
  borderColor: colors.lightGray,
  color: colors.lightGray,
  hoverBgColor: colors.grey,
  style: { width: "30px", height: "30px", padding: "1rem" },
}

export default buttonStyle
